This line was added to .bashrc in the user device to make an alias for connecting to the drone by the bash script:

alias cleverconnect="~/.clever_drone_connect.bash"

These lines were added to .bashrc in the raspberry pi board to make aliases for scripts of client mode and accesspoint switching:
alias clientmode='~/.to_client.bash'
alias APmode='~/.to_accesspoint.bash'
